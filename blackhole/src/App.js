// import React from 'react';
// import {connect } from 'react-redux';


// class App extends React.Component {
// // CDM - check if user has token, if the token is in localstorage, login using this.props.login(JSON.parse(localStorage.getItem('username')));

// //Display Nav bar with Username, hole/home, post, logout if logged in
// //if logged out display nav bar with register, login, options.

// //Render out the app, if user is logged in, display an internal App Home component with all of the logged in functionality,
// //Else display the Registration/login page



// const mapStateToProps = state => {
// return {
// username: this.state.login.username,
// fetchingPosts: this.state.login.fetchingPosts,
// postsFetched: this.state.login.postsFetched,
// isLogingIn: this.state.login.isLogingIn,
// isLoggedIn: this.state.login.isLoggedIn,


// };


// };

// export default connect( mapStateToProps, {homepage, login, logout})(App);